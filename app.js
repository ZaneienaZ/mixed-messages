const messages = require('./messages');
let result = messages.tellMeMyFortune
//console.log(messages);
console.log(messages());

//This is the function to call in terminal. Call by using node app
//to get a randomized fortune
